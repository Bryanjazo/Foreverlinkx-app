import { StyleSheet } from "react-native";

const ButtonCardsStyles = StyleSheet.create({
  buttonContainer: {
    backgroundColor: "#fff",
    borderRadius: 8,
    elevation: 2,
  },
});

export default ButtonCardsStyles;

import { StyleSheet } from "react-native";

export const BookHomeStyles = StyleSheet.create({
  bookHeaderContainer: {
    justifyContent: "center",
    marginBottom: 20,
    maxHeight: "100%",
  },
  bookingInfo: {},
});
